# Variables de entorno para desarrollo local
# Copia este archivo como .env.local y no lo subas al repositorio

# URL del backend (en producción, apunta a tu servidor desplegado)
VITE_API_URL=http://localhost:8000/api
